{
    "title": "SSL Certificate Expiry Checker Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "hosts": {
            "title": "Hosts",
            "type": "array",
            "editor": "stringList",
            "description": "Hostnames to check, without protocol (e.g. \"example.com\" or \"example.com:8443\" for a non-standard port). One check is billed per host.",
            "prefill": ["google.com"]
        }
    }
}
